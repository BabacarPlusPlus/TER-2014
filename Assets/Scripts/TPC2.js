
// Require a character controller to be attached to the same game object
@script RequireComponent(CharacterController)

public var speed = 5;


function Update() {
	
	
//	transform.rigidbody.AddForce(Vector3.forward * speed);

}

